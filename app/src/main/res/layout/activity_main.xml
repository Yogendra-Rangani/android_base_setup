<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

        <com.google.android.material.textfield.MaterialAutoCompleteTextView
            android:id="@+id/tvTransactionFilter"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:background="@null"
            android:cursorVisible="false"
            android:drawableEnd="@drawable/ic_arrow_down"
            android:drawableTint="@color/app_primary"
            android:focusable="true"
            android:focusableInTouchMode="false"
            android:gravity="start|center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="0dp"
            android:textColor="@color/app_primary"
            android:textColorHint="@color/app_primary"
            android:textSize="16sp"
            app:layout_constraintTop_toTopOf="parent" />


        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvAch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="10dp"
            android:drawableStart="@drawable/ic_arrow_down"
            android:drawablePadding="5dp"
            android:gravity="center_vertical"
            android:text="ACH"
            android:textColor="@color/app_primary"
            android:textSize="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvTransactionFilter" />

        <FrameLayout
            android:id="@+id/bottomSheetContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            app:layout_constraintTop_toBottomOf="@+id/tvAch">

        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>